# Maintainer Guide

Keep scanner state transitions explicit, isolate browser APIs behind small helpers, and avoid putting camera-frame data into logs or persistent storage. When changing scan timing, test both fast detections and intermittent camera input.
