# Browser Support Notes

Test the scanner in current Chrome, Edge, Firefox, and Safari releases.

Check camera permission prompts, autoplay restrictions, Web Audio support, requestAnimationFrame behavior, responsive video sizing, and model loading from `/models`.

For unsupported features, provide a clear fallback message instead of silently failing.