# Camera Permission Matrix

Document expected behavior for permission states: granted, denied, dismissed, unavailable, and revoked after grant.

The UI should explain what is happening without implying that camera data leaves the device. Every state should provide a clear recovery path where the browser allows it.
