# Reset behavior QA

Press reset before detection, during the detected phase, during scanning, and after completion. Each case should clear progress, overlays, generated decorations, and active anthem playback without leaving stale UI state behind.