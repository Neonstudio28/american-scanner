# Stale detection QA

Move a detected face out of frame and keep it absent long enough to cross the miss threshold. Confirm the overlay disappears and the scanner returns to the idle state instead of presenting stale detection data.