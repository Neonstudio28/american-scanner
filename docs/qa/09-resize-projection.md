# Resize and projection QA

Test the face overlay at common 16:9 and 4:3 camera dimensions, then resize the browser while detection is active. Confirm mirrored coordinates stay aligned with the video crop rather than drifting toward the original viewport dimensions.