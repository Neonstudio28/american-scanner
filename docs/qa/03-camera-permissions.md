# Camera permission QA

Verify first load requests video only, denial leaves the application usable, and no microphone permission is requested. Recheck after browser permission is revoked while the page is open.