# Unmount cleanup QA

Navigate away while detection, scanning, or completion effects are active. Verify animation frames and intervals are cancelled and camera tracks are stopped so the previous page does not keep processing work.