# Page visibility QA

When the tab is backgrounded during a scan, check that animation and timers do not leave the UI in an impossible phase. On return, verify the displayed state remains internally consistent and reset remains available.