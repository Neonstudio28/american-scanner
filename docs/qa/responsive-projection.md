# Face-box projection QA

Validate the SVG overlay against the mirrored, cover-cropped video at these input ratios:

- 16:9 landscape
- 4:3 landscape
- square input
- portrait input
- resized desktop viewport
- narrow mobile viewport

The projected box must stay aligned with the visible face after mirroring and cropping. Overlay labels must remain inside the viewport.
