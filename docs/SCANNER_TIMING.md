# Scanner Timing

The current flow uses a short confirmation window before scanning and a fixed scan duration. Any timing change should be tested with delayed camera startup, intermittent detection, and reset during scanning to ensure timers are cleared correctly.