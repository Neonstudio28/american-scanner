# Privacy boundary

Camera frames are consumed in the browser for detection. Avoid adding telemetry that captures frames, face crops, model outputs, or other biometric data. Any future analytics should record aggregate application events only unless a separate privacy review explicitly approves more.