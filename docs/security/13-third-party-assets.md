# Third-party asset review

Review CDN-loaded libraries and static media before release. Pin versions where practical, prefer trusted origins, and avoid introducing scripts that can inspect camera frames without a documented product requirement.