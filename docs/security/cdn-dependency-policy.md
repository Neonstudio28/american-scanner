# CDN dependency policy

Third-party browser scripts must be pinned to an explicit version and reviewed before changing. A dependency loaded from a CDN should have a documented purpose, failure behavior, and upgrade path.