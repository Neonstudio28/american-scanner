# Detection Confidence

Treat face detection as an interaction signal, not an identity assertion. Tune thresholds for stable UX and document any fallback behavior. Never present heuristic detection as a factual biometric judgment.