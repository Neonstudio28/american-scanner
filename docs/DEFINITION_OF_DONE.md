# Definition of Done

A scanner change is complete when it has a clear user-facing purpose, does not leak camera data, handles failure gracefully, keeps cleanup paths intact, passes lint/build checks, and has documentation or tests where the behavior is non-obvious.