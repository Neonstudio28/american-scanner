# Secure Context Requirement

Camera access depends on browser security rules. Production deployments should use HTTPS, while local development may use localhost. Never weaken browser security requirements to make camera access work.