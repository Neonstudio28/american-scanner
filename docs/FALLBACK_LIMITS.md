# Fallback Detection Limits

The brightness or skin-tone fallback is only a coarse signal for demo resilience. It must not be described as identity, nationality, ethnicity, or demographic classification.