# Camera recovery

If the active video track ends unexpectedly, the scanner should return to a recoverable idle state instead of retaining stale detection state. QA should cover device unplug, browser permission revocation, and camera hardware contention.