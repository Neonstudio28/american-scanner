# Content Security Policy Planning

Before adding a Content Security Policy, inventory the CDN script, media assets, model requests, and inline resources. The policy should reduce third-party execution risk without silently breaking the scanner.