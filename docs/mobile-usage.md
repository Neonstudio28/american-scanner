# Mobile Usage

The app should be usable on narrow touch screens.

Verify viewport scaling, camera aspect-ratio cropping, readable status text, touch-target sizing, and recovery when a mobile browser suspends the camera or audio context.
