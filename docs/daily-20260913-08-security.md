# Third-party asset security

CDN-hosted scripts and model assets are part of the scanner trust boundary. Production deployments should pin versions where possible, use HTTPS, review upstream changes, and avoid logging sensitive detector or camera data.