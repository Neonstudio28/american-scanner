# Privacy Notice

The scanner requests camera access only when the page needs live video. Treat camera frames as ephemeral client-side data and do not upload them unless a future feature explicitly documents that behavior.
