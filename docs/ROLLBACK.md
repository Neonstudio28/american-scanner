# Rollback Procedure

If a release causes camera, model, or runtime regressions, revert to the last verified build, record the failure, and preserve the failing scenario for a regression test before re-releasing.