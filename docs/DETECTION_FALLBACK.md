# Detection Fallback

When the face detector is unavailable, the fallback path should be treated as a coarse availability check, not an equivalent biometric detector. Keep user-facing wording clear about reduced confidence and never imply that approximate pixel heuristics identify a person.