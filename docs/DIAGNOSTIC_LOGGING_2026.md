# Diagnostic Logging

Log only actionable runtime metadata: phase, permission outcome, model-load status, and non-sensitive error categories. Never log camera frames, face coordinates tied to users, or raw media data.