# Cross-Browser Smoke Test

Run the basic scanner flow in Chromium-based browsers, Firefox, and Safari where supported. Record camera permission, model loading, overlay alignment, and audio differences separately.