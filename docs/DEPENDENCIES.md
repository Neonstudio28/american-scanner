# Dependency Notes

Keep React, React DOM, Vite, and Oxlint versions aligned with the supported toolchain. Review browser-facing dependency updates for bundle-size changes and security advisories before upgrading.