# Dependency policy

Keep runtime dependencies minimal. Before adding a package, confirm the browser API or existing dependency cannot solve the requirement, review maintenance health and license compatibility, and document why the dependency is needed.