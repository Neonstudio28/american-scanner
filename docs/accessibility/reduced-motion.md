# Reduced-motion behavior

The scanner should respect `prefers-reduced-motion: reduce`.

Animations that are decorative should be shortened or disabled without removing important status information. Scan progress, detection state, completion state, and retry controls must remain understandable without motion.
