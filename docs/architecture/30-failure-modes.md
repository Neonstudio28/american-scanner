# Failure-mode inventory

Primary recoverable failures are camera permission denial, camera track loss, unavailable detector library, detector model load failure, inference exceptions, blocked audio playback, and component unmount during an active session. Each should leave a path back to a usable application state.