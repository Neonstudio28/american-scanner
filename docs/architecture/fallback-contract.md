# Fallback contract

The fallback detector is a degraded signal, not equivalent to face-api detection. Product copy and telemetry must identify fallback mode so a heuristic result is never presented as high-confidence biometrics.