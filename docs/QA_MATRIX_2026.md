# QA Matrix

Minimum regression pass: desktop camera permission, mobile camera permission, detector load, fallback path, scan completion, reset during scan, audio blocked, missing decorative asset, keyboard navigation, reduced motion, and narrow viewport rendering.