# Model Loading

Load detector models asynchronously, expose progress or failure state, and keep the interface recoverable when model assets are unavailable. Remote model failures should not freeze the scanner.