# Fallback Detection

The fallback detector is heuristic and must be treated as an availability fallback rather than an authoritative biometric model.

It should never be presented as medically, scientifically, or security accurate. Keep user-facing copy clear that the experience is entertainment-oriented.
