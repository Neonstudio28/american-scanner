# Model Failure Runbook

If the detector model fails to load, keep the UI usable, record a non-sensitive diagnostic message, and use the documented fallback path. Do not repeatedly retry without bounds.