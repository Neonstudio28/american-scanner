# QA Smoke Tests

Verify the page loads, camera permission can be requested, detection can start, a scan can complete, reset returns to idle, and the page remains usable when camera or audio is unavailable.