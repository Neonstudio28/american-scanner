# Fallback Copy

When detector assets are unavailable, tell the user what failed and what they can do next. Do not present a heuristic fallback as equivalent to the face detector or imply that it can establish identity, nationality, or any sensitive trait.