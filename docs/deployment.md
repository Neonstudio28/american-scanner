# Deployment Checklist

Before deploying:

1. Run `npm run lint`.
2. Run `npm run build`.
3. Confirm `/models` contains every model asset referenced by the client.
4. Confirm required public audio and image assets are available.
5. Verify camera permissions on the deployed origin.
6. Test the production build on a desktop and mobile browser.
