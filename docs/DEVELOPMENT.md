# Development Notes

American Scanner is a Vite application using React 19 and Oxlint.

## Useful commands

- `npm run dev` starts the local development server.
- `npm run build` creates a production build.
- `npm run lint` runs Oxlint.
- `npm run preview` serves the production build locally.

The project uses ES modules and keeps the main application code under `src/`.
