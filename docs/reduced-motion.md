# Reduced Motion

Decorative motion should respect `prefers-reduced-motion: reduce`.

The scanner's functional flow must remain available when motion is disabled. Decorative bouncing elements, glitch effects, and nonessential transitions should be reduced or removed without affecting scan state.
