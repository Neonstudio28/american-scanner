# Observability

Log only operational events such as model-load failure, camera start failure, and feature lifecycle transitions. Never log raw frames, face boxes, or other unnecessary biometric-derived data. Keep logs useful for debugging without creating a second sensitive data store.