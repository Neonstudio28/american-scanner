# Recovery Actions

Permission errors should explain how to re-enable the camera. Model errors should offer retry or reload. Asset and playback errors should degrade gracefully without blocking the scan UI.