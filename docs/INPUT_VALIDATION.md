# Input Validation

Validate media readiness, detector results, scan state transitions, and asset availability before consuming them. Invalid or incomplete state should fail closed and return the UI to a recoverable state.