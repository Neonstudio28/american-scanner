# Contribution Workflow

For changes that affect camera access, detection, audio, or scan state: make the smallest coherent change, run lint/build, test the affected browser flow, and explain user-facing impact in the commit message or PR description.