# Dependency Upgrade Policy

Upgrade React, Vite, Oxlint, and detector dependencies deliberately. Review release notes, run lint/build checks, and manually test camera permissions and scan completion after upgrades.