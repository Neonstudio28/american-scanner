# Browser compatibility checklist

Validate camera permissions, `requestAnimationFrame`, Web Audio availability, video playback, CSS mirroring, and model loading on current Chromium, Firefox, and Safari releases. Unsupported APIs should fail into a readable recovery state.