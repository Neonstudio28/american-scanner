# Camera privacy boundary

The scanner should process camera frames locally and avoid persisting frames or biometric outputs unless a separate feature explicitly requires it. Error logs should not include captured image data or raw detector payloads.