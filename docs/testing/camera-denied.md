# Camera denied case

Expected behavior: the UI remains usable, explains why scanning cannot start, does not loop permission requests, and provides a manual retry path after the browser permission changes.