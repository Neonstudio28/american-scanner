# Multiple faces

Define expected behavior when multiple faces appear in frame. The UI should avoid implying that the detector has identified one specific person unless selection behavior is explicit.