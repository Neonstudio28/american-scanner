# Model timeout case

Test that a slow or unreachable model does not leave the startup screen in an indefinite loading state. After the timeout, provide a recoverable error and keep the page responsive.