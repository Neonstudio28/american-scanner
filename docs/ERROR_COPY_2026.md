# Error Copy

Errors should explain what failed, whether the user can retry, and whether camera or audio permissions are involved. Avoid exposing stack traces or implementation details in user-facing messages.