# Release preflight

Before deployment: install from a clean lockfile state, run lint, run a production build, verify `/models` assets are present, verify required media assets exist, and manually test camera permission plus reset behavior on a supported browser.