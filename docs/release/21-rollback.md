# Rollback guidance

If a release breaks camera startup or scanning, revert to the last known-good deployment and preserve the failing build identifier for diagnosis. Do not ask users to bypass browser camera security controls.