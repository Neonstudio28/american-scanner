# Release preflight

Before shipping, run a production build, lint the source, verify model and media assets exist, test camera permission recovery, and exercise reset during an active scan. Confirm that optional audio failure does not block completion.